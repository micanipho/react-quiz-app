# react-quiz-app
Develop a single-page React application that implements a multiple-choice quiz. The application must feature a start screen with a "Start" button. Upon initiation, the user will navigate through a ser
